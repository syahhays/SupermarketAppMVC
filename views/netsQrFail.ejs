<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title || 'NETS QR Error' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container my-5">
    <div class="card p-4">
      <h3 class="mb-3">NETS QR Error</h3>
      <p class="text-danger mb-2"><%= errorMsg || 'Unable to generate NETS QR.' %></p>
      <div class="text-muted small">
        Response code: <%= responseCode || 'N.A.' %>
      </div>
      <% if (instructions) { %>
        <div class="text-muted small mt-1"><%= instructions %></div>
      <% } %>
      <div class="mt-3">
        <a class="btn btn-outline-secondary" href="/checkout">Back to checkout</a>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
