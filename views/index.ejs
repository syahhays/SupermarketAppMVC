<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Welcome — Groceria</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">

  <style>
    .hero {
      padding: 6rem 1rem;
      background: linear-gradient(90deg, #f8f9fa 0%, #ffffff 100%);
      border-radius: 0.5rem;
      box-shadow: 0 8px 24px rgba(0,0,0,0.06);
    }
    .hero h1 { font-weight:700; letter-spacing: -0.5px; }
    .cta-row .btn { min-width:140px; }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <main class="container my-5">
    <!-- HERO -->
    <div class="hero text-center">
      <img src="/images/groceria-logo.png" alt="Groceria" class="hero-logo mb-3" />
      <h1 class="display-5 mb-3">Welcome to Groceria</h1>
      <p class="lead mb-4">Fresh groceries, everyday essentials and easy checkout — all in one place.</p>

      <div class="d-flex justify-content-center gap-3 cta-row mb-3">
        <% if (user) { %>
          <a href="/shopping" class="btn btn-primary btn-lg">Start Shopping</a>
          <a href="/cart" class="btn btn-outline-secondary btn-lg">View Cart</a>
        <% } else { %>
          <a href="/register" class="btn btn-primary btn-lg">Get Started</a>
          <a href="/login" class="btn btn-outline-secondary btn-lg">Sign In</a>
        <% } %>
      </div>
    </div>

    <!-- QUICK LINKS -->
    <div class="row mt-5">
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>Shop</h5>
          <p class="text-muted small">Browse products and add to cart.</p>
          <a href="/shopping" class="stretched-link"></a>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>Orders</h5>
          <p class="text-muted small">View your order history and details.</p>
          <a href="/orders" class="stretched-link"></a>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card p-3 h-100">
          <h5>Admin</h5>
          <p class="text-muted small">Inventory and order management (admins).</p>
          <a href="/inventory" class="stretched-link"></a>
        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>